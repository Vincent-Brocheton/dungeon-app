{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null); case \"$branch\" in main|master) printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Sur %s : cree une branche avant de modifier des fichiers (git checkout -b feat/nom ou fix/nom).\"}}' \"$branch\" ;; *) printf '{}' ;; esac"
          }
        ]
      }
    ]
  }
}
